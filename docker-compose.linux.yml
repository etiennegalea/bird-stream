services:
  backend:
    ports: !reset []
    environment:
      VIDEO_SOURCE: /dev/video0
    devices:
      - "/dev/video0:/dev/video0"
    network_mode: host
    networks: !reset null

  coturn:
    network_mode: host
    networks: !reset null

networks:
  app-network:
    enable_ipv6: true
